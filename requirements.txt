# SDA Pathfinder — runtime dependencies (cross-platform).
# RSA wheels are NOT listed here — the launcher picks them up from
# vendor/<platform>/ at install time so each OS gets the correct binary.

fastapi==0.136.3
uvicorn==0.47.0
pydantic==2.11.7

# Cisco pyATS / Genie (parsers for show commands)
genie==26.3

# Misc runtime deps used by helper modules
pysnmp==6.1.4
asn1crypto==1.5.1
